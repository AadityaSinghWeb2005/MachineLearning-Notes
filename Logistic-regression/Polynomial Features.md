
**Introduction :** So Logistic Regression condition was that Data should show Linear Behavior 
But what if Its Show Polynomial Behavior then in that case 
If A Data is Showing Polynomial Behavior and We need to apply Logistic Regression Then We use Polynomial Logistic regression (in which we First Convert the the columns into Polynomials with Degrees Eg. X Input column convert into X^0 , X^1 , X^2 for Degree = 3 )
